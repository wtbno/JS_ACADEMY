// ? Valor para verdadeiro  : Valor para falso

const UserScore = 999;

const UserNivel =
  UserScore >= 999 ? "Pontuação acima de 999" : "Pontuação insuficiente";

// if (UserScore >= 999) {
//     console.log("Pontuação acima de 999");

// }else (console.log("Pontuação insuficiente")
// )
